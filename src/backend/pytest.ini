[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -v --strict-markers -n auto
markers =
    unit: unit tests
    integration: integration tests
    slow: slow running tests
